# Secure API (Flask)

A Flask API implementing basic security features like authentication, hashing, and token-based access.

## Features
- User registration
- Login with password hashing
- Token-based authentication
- Protected endpoint
- Input validation

## Endpoints
- POST /register
- POST /login
- GET /secure-data

## Run
```bash
pip install flask
python app.py
